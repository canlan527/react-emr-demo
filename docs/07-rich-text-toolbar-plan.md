# 富文本工具栏计划

## 目标

为 `/medical-record` 电子病历 Canvas 编辑器增加常见电子病历富文本工具栏。

本阶段先制定需求和架构边界，不实现 UI 和代码。

富文本工具栏的目标不是一次性复刻完整 Word，而是在当前 Canvas Word 基础上，逐步支持临床文书常用的格式化、模板插入和文档操作能力。

## 当前前提

当前电子病历编辑器已经具备：

- Canvas 文本绘制。
- 自动换行。
- 分页。
- 中文输入法。
- 光标移动。
- 鼠标选区。
- 复制、剪切、粘贴、删除。
- 撤销、重做。
- 右键菜单。
- 加载病历文档。

当前主要限制：

- 编辑态核心仍是纯文本字符串。
- 文档结构会先通过 `documentToPlainText()` 转成纯文本。
- 标题和正文已有不同绘制样式，但还不是完整富文本模型。
- 选区仍基于文本索引和可视位置的组合模型。

富文本工具栏会推动文档模型从纯文本升级为 block + run 模型。

## 工具栏范围

### 基础文本格式

一期建议支持：

- 加粗。
- 下划线。
- 字体颜色。
- 背景高亮色。
- 字号。
- 清除格式。

二期可支持：

- 斜体。
- 删除线。
- 字体族。
- 上标。
- 下标。
- 格式刷。

### 段落格式

一期建议支持：

- 左对齐。
- 居中。
- 右对齐。

二期可支持：

- 两端对齐。
- 首行缩进。
- 增加缩进。
- 减少缩进。
- 行距。
- 段前距。
- 段后距。

### 常用编辑操作

一期建议支持：

- 撤销。
- 重做。
- 复制。
- 剪切。
- 粘贴。
- 删除。
- 全选。

这些能力已有底层实现，工具栏只需要复用现有编辑命令。

### 病历字段插入

一期建议支持插入：

- 姓名。
- 性别。
- 年龄。
- 科室。
- 床号。
- 住院号。
- 入院日期。
- 诊断。

字段插入规则：

- 如果有选区，字段内容替换选区。
- 如果没有选区，字段内容插入到光标位置。
- 插入操作进入撤销/重做历史栈。

### 病历结构模板

一期建议支持插入：

- 主诉。
- 现病史。
- 既往史。
- 体格检查。
- 初步诊疗计划。
- 病程记录。

二期可支持：

- 辅助检查。
- 诊断依据。
- 鉴别诊断。
- 出院小结。
- 手术记录。
- 会诊记录。

模板插入规则：

- 模板作为文本块插入。
- 插入后保留基本段落结构。
- 后续富文本模型稳定后，再升级为结构化 block 插入。

### 常用短语

一期建议支持：

- 无明显诱因。
- 生命体征平稳。
- 神志清楚。
- 精神可。
- 皮肤黏膜无黄染。
- 心肺腹查体未见明显异常。
- 继续观察病情变化。
- 遵医嘱治疗。

二期可支持：

- 按科室维护短语。
- 搜索短语。
- 最近使用短语。
- 用户自定义短语。

### 医疗符号与单位

一期建议支持插入：

- ℃
- mmHg
- 次/分
- mg
- ml
- bid
- tid
- qd
- qn
- prn

二期可支持：

- 单位面板。
- 医嘱缩写面板。
- 常用检验项目缩写。

### 文档级操作

一期建议保留或提供入口：

- 加载病历文档。
- 页面缩放。

二期可支持：

- 保存。
- 本地持久化。
- 打印。
- 导出 PDF。
- 查找替换。
- 单页视图。
- 连续页视图。
- 适合宽度。

## 一期需求边界

一期目标是让电子病历编辑器具备可见的富文本编辑入口，并验证富文本模型是否能承载后续扩展。

一期包含：

- 固定在文档上方的工具栏。
- 撤销、重做。
- 加粗。
- 下划线。
- 字体颜色。
- 背景高亮色。
- 字号。
- 清除格式。
- 左对齐。
- 居中。
- 右对齐。
- 插入当前日期。
- 插入病历字段。
- 插入病历结构模板。
- 插入常用短语。
- 插入医疗符号和单位。

一期不包含：

- 表格。
- 图片。
- 电子签名。
- 页眉页脚。
- 打印。
- PDF 导出。
- 多级列表。
- 格式刷。
- 复杂模板管理。
- 用户自定义短语管理。
- 与后端接口保存。

## 二期需求边界

二期在一期富文本模型稳定后再做。

二期方向：

- 表格。
- 图片和签名插入。
- 页眉页脚。
- 打印。
- PDF 导出。
- 查找替换。
- 本地持久化或后端保存。
- 多级列表。
- 行距和段落间距。
- 首行缩进和悬挂缩进。
- 科室模板管理。
- 常用短语管理。
- 结构化病历字段绑定。

## 文档模型升级

### 当前模型问题

当前编辑器核心状态类似：

```ts
type PlainEditorState = {
  text: string;
  cursor: number;
};
```

这种模型适合纯文本，但不适合富文本。

问题包括：

- 无法表达同一段文字中不同字号、颜色、加粗状态。
- 无法表达段落对齐。
- 无法表达插入模板后的结构。
- 撤销/重做只能恢复纯文本，不能恢复样式。
- 复制粘贴只能处理纯文本。

### 建议模型

建议升级为 block + run：

```ts
type RichTextDocument = {
  blocks: DocumentBlock[];
};

type DocumentBlock = {
  id: string;
  type: 'heading' | 'paragraph' | 'fieldGroup';
  align: 'left' | 'center' | 'right';
  indent?: number;
  runs: TextRun[];
};

type TextRun = {
  id: string;
  text: string;
  marks: TextMarks;
};

type TextMarks = {
  bold?: boolean;
  italic?: boolean;
  underline?: boolean;
  strike?: boolean;
  color?: string;
  backgroundColor?: string;
  fontSize?: number;
  fontFamily?: string;
};
```

### 模型职责

`DocumentBlock` 负责：

- 段落类型。
- 段落对齐。
- 段落缩进。
- 段落级间距。

`TextRun` 负责：

- 连续文本。
- 字符级样式。

`TextMarks` 负责：

- 加粗。
- 下划线。
- 字号。
- 字体颜色。
- 背景色。

## 选区与富文本操作

### 无选区

当没有选区时：

- 点击加粗、颜色、字号等按钮，不立即修改已有文字。
- 编辑器记录一个 `activeMarks`。
- 后续输入的文字使用 `activeMarks`。
- 光标位于已有文本中时，工具栏展示光标所在 run 的样式。

### 有选区

当有选区时：

- 工具栏操作直接应用到选区范围。
- 如果选区跨越多个 run，需要拆分 run。
- 操作完成后选区保留，便于继续应用其它格式。
- 操作进入撤销/重做历史栈。

### 跨段选区

跨段选区需要支持：

- 对所有涉及的 run 应用字符样式。
- 对所有涉及的 block 应用段落样式。
- 复制时输出纯文本。

二期可考虑复制富文本格式。

## Canvas 排版影响

富文本后，排版不再是一个纯字符串。

新的排版需要按 run 测量：

- 每个 run 根据 `TextMarks` 生成真实 canvas font。
- 使用 `ctx.measureText()` 计算字符宽度。
- 同一视觉行可能包含多个 run。
- 同一视觉行可能包含多种字号。
- 行高应取该行所有 run 中最大字号对应的行高。

建议新增或重构：

- `richTextDocument.ts`
- `richTextEditing.ts`
- `richTextLayout.ts`
- `richTextSelection.ts`
- `canvasRenderer.ts`
- `toolbarCommands.ts`

## 撤销/重做影响

当前历史栈保存：

- `text`
- `cursor`

富文本后建议保存：

- `document`
- `selection`
- `activeMarks`

建议结构：

```ts
type RichHistorySnapshot = {
  document: RichTextDocument;
  selection: EditorSelection | null;
  activeMarks: TextMarks;
};
```

历史栈仍保持：

- `past`
- `future`

加载文档时清空历史栈。

每次工具栏命令都应该进入历史栈。

## 工具栏状态

工具栏按钮需要根据当前光标或选区更新状态。

一期简化规则：

- 无选区时，显示光标所在 run 的样式。
- 有选区时，显示选区起点所在 run 的样式。
- 如果选区内样式不一致，暂不显示混合态。

二期增强规则：

- 支持混合态。
- 支持半选态。
- 支持多段落对齐状态判断。

## UI 交互建议

工具栏位置：

- 固定在电子病历文档上方。
- 不随页面整体顶部固定。
- 跟随电子病历模块布局。

工具栏分组：

- 历史操作：撤销、重做。
- 文本样式：加粗、下划线、字号、颜色、背景色、清除格式。
- 段落样式：左对齐、居中、右对齐。
- 病历插入：字段、模板、常用短语、单位符号。
- 文档操作：加载文档、缩放。

UI 组件选择：

- 一期可以继续使用本地 CSS 和原生按钮。
- 如果后续工具栏弹层、下拉、颜色面板较多，可以考虑引入 Ant Design。
- 引入 UI 库前要评估包体积和样式覆盖成本。

## 验收标准

一期验收：

- 工具栏在 `/medical-record` 页面可见。
- 选中文本后，可以应用加粗、下划线、字体颜色、背景高亮、字号。
- 无选区时，设置样式后继续输入，新文字使用该样式。
- 左对齐、居中、右对齐能作用于当前段落或选区段落。
- 字段、模板、短语、单位可以插入到光标位置。
- 有选区时，插入内容替换选区。
- 所有工具栏操作支持撤销/重做。
- 中文输入法仍能正常输入。
- 选区高亮仍与文字位置一致。
- Canvas 分页和自动换行仍正常。

二期验收：

- 支持表格、图片、签名或 PDF 导出等增强能力时，不破坏一期编辑能力。
- 复杂富文本复制粘贴有明确策略。
- 文档模型可以保存和重新加载。

## 风险点

- 富文本会显著增加排版复杂度。
- 多字号混排会影响行高和光标命中。
- run 拆分和合并容易产生边界 bug。
- 标点避头逻辑需要适配跨 run 文本。
- 选区模型如果继续临时扩展，维护成本会升高。
- 撤销/重做从纯文本升级为文档快照后，需要控制快照大小。

## 建议实施顺序

1. 定义 `RichTextDocument`、`DocumentBlock`、`TextRun`、`TextMarks`。
2. 从现有病历文档生成富文本 document。
3. 实现富文本到纯文本的映射，保证复制和旧逻辑可过渡。
4. 重构 layout，让视觉行可以包含多个 run。
5. 重构 selection，让选区可以定位到 block/run/offset。
6. 重构 render，按 run 绘制文本样式。
7. 接入 toolbar command，但先不用复杂 UI。
8. 再实现真实工具栏 UI。
9. 更新撤销/重做历史快照。
10. 做中文输入、选区、复制粘贴、撤销重做回归。
